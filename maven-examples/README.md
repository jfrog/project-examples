# Maven Examples

## Prerequisites

Ensure the JDK is installed locally.

Apt example:
```sh
sudo apt install openjdk-21-jdk-headless
```

## Running Locally

```sh
cd ./maven-example
./mvnw package
```
