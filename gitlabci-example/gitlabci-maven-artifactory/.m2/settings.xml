<?xml version="1.0" encoding="UTF-8"?>
<settings xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.1.0 http://maven.apache.org/xsd/settings-1.1.0.xsd" xmlns="http://maven.apache.org/SETTINGS/1.1.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <servers>
    <server>
      <username>${env.ARTIFACTORY_USER}</username>
      <password>${env.ARTIFACTORY_PASS}</password>
      <id>central</id>
    </server>
    <server>
      <username>${env.ARTIFACTORY_USER}</username>
      <password>${env.ARTIFACTORY_PASS}</password>
      <id>snapshots</id>
    </server>
  </servers>
  <profiles>
    <profile>
      <repositories>
        <repository>
          <snapshots>
            <enabled>false</enabled>
          </snapshots>
          <id>central</id>
          <name>${env.MAVEN_REPO_KEY}</name>
          <url>${env.ARTIFACTORY_URL}/${env.MAVEN_REPO_KEY}</url>
        </repository>
        <repository>
          <snapshots />
          <id>snapshots</id>
          <name>${env.MAVEN_REPO_KEY}</name>
          <url>${env.ARTIFACTORY_URL}/${env.MAVEN_REPO_KEY}</url>
        </repository>
      </repositories>
      <pluginRepositories>
        <pluginRepository>
          <snapshots>
            <enabled>false</enabled>
          </snapshots>
          <id>central</id>
          <name>${env.MAVEN_REPO_KEY}</name>
          <url>${env.ARTIFACTORY_URL}/${env.MAVEN_REPO_KEY}</url>
        </pluginRepository>
        <pluginRepository>
          <snapshots />
          <id>snapshots</id>
          <name>${env.MAVEN_REPO_KEY}</name>
          <url>${env.ARTIFACTORY_URL}/${env.MAVEN_REPO_KEY}</url>
        </pluginRepository>
      </pluginRepositories>
      <id>artifactory</id>
    </profile>
  </profiles>
  <activeProfiles>
    <activeProfile>artifactory</activeProfile>
  </activeProfiles>
</settings>
